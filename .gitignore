# Planning artifacts (local only, not tracked)
plans/
reviews/
changelog/

# Go
/provisioning-api/bin/
/provisioning-api/provisioning-api
*.test
*.out

# Node / Cloudflare Pages
node_modules/
dist/
.wrangler/
.env
.env.*
!.env.example

# Bot config — account-specific GitHub App IDs (the .example template is tracked)
.claude/bot/bot.env

# Personal local overrides (sandbox exclusions, per-machine permission tweaks)
.claude/settings.local.json

# OS
.DS_Store
